@{
    ViewBag.Title = "Asistencia del mecánico";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="content-box">
    <h1>Escanear el código QR</h1>
    <img src="@Url.Action("Generar_QR", "Inicio")" alt="Código QR para Asistencia" class="img-fluid" />
    <div class="text-center mt-4">
        <a href="@Url.Action("GestionarMecanicos", "Inicio")" class="btn btn-secondary">Volver</a>
    </div>
</div>

<style>
    .content-box {
        background-color: #ffffff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        margin: 20px auto;
        text-align: center;
    }

        .content-box h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .content-box .btn {
            margin-top: 20px;
        }
</style>
